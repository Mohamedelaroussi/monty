#include "monty.h"

/**
 * f_nop - nothing
 * @stack: pointing to stack head
 * @count: line number
 * Return: no return
 */

void f_nop(stack_t **stack, unsigned int count)
{
	(void) count;
	(void) stack;
}
